cloudflow {
  runtimes {
    flink {
      config {
         flink.state.backend = filesystem
         flink.state.checkpoints.dir = "file:///tmp/flinkcps"

      }
    }
  }
}

cloudflow.streamlets.processor.config {
  local.web = on
  rest.port = 5000
}